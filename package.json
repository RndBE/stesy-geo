{
  "name": "stesygeo",
  "private": true,
  "description": "STESY GEO — Sistem Monitoring Konsolidasi Tanah",
  "scripts": {
    "setup": "npm --prefix server install && npm --prefix web install",
    "seed": "npm --prefix server run seed",
    "build": "npm --prefix web run build",
    "start": "npm --prefix server start",
    "dev:api": "npm --prefix server run dev",
    "dev:web": "npm --prefix web run dev",
    "simulate": "npm --prefix server run simulate --",
    "test": "npm --prefix server test",
    "typecheck": "npm --prefix server run typecheck && cd web && npx tsc --noEmit"
  }
}
